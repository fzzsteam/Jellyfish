# Jellyfish 使用手册（新手主流程详解）

> Jellyfish 是一个 **AI 驱动的短剧 / 短视频生产工作台**。
> 你只要丢进一段剧本文字，平台就能带你一步步走完：
> **录入剧本 → 自动拆出镜头 → 在镜头详情四步里确认每个镜头的角色/场景/道具/对白 → 给镜头出关键帧 → 生成视频**。
>
> 它最核心的本事是「**保证一致性**」：让同一个角色、同一个场景在所有镜头里长得一样、不跑偏。
>
> 本手册按**主流程**讲，并且**把每个页面上的按钮一个个讲清楚**，新手照着点就能上手。

---

## 目录

1. [先理解 5 个核心概念](#1-先理解-5-个核心概念)
2. [主流程全景图](#2-主流程全景图)
3. [步骤一：项目列表页 —— 新建并管理作品](#3-步骤一项目列表页--新建并管理作品)
4. [步骤二：项目工作台 + 章节页 —— 录入剧本](#4-步骤二项目工作台--章节页--录入剧本)
5. [步骤三：分镜列表页 —— 一键拆出镜头](#5-步骤三分镜列表页--一键拆出镜头)
6. [步骤四：镜头详情页 —— 基础信息 / 提取确认 / 生成视频 / 视频结果](#6-步骤四镜头详情页--基础信息--提取确认--生成视频--视频结果)
7. [步骤五：资产页 —— 常用一致性补强环节](#7-步骤五资产页--常用一致性补强环节)
8. [步骤六：镜头详情 —— 生成视频 + 查看结果](#8-步骤六镜头详情--生成视频--查看结果)
9. [步骤七：任务中心 —— 看进度、拿成片](#9-步骤七任务中心--看进度拿成片)
10. [界面导航速查](#10-界面导航速查)
11. [常见问题 FAQ](#11-常见问题-faq)

---

## 1. 先理解 5 个核心概念

把这套「套娃」关系理解了，整个平台就通了：

```
项目 (Project)          一部作品
 └─ 章节 (Chapter)       一段完整剧本，比如「第1集」
     └─ 镜头 (Shot)      剧本被拆出来的一个个分镜
         ├─ 镜头语言：景别、机位、运镜、时长、动作拍点
         ├─ 对白：这个镜头里的台词
         └─ 关键帧：首帧 / 尾帧 / 关键帧
```

**资产**是「跨镜头复用的素材库」，是保证一致性的关键：

| 资产 | 通俗解释 |
| --- | --- |
| **角色 (Character)** | 剧中人物（属于某个项目），带形象图。 |
| **演员 (Actor)** | 可跨项目复用的「演员形象库」，相当于一张脸的底模，角色可以绑定它。 |
| **场景 (Scene)** | 故事发生的地点环境（客厅、街道…）。 |
| **道具 (Prop)** | 镜头里出现的物件（手机、刀…）。 |
| **服装 (Costume)** | 角色的服装造型。 |

**两个状态词，务必分清：**

- **镜头状态 `status`**：只有「待确认 (pending)」「已就绪 (ready)」两种。`ready` = 这个镜头的**信息确认工作做完了**。
- **视频准备度 `video-readiness`**：单独判断「这个镜头**现在能不能出视频**」（关键帧、参考图、参数齐不齐）。

> 一句话：**`ready` = 准备做完了；视频准备度 = 生成素材齐了。** 两者各管各的。

---

## 2. 主流程全景图

```
①建项目 → ②录入剧本 → ③项目工作台进入分镜列表
   → ④镜头详情四步（基础信息 → 提取确认 → 生成视频 → 视频结果）
   → ⑤在镜头详情生成视频并查看结果
   → ⑥任务中心（看进度、下载成片）
```

平台把工作清楚地分成三个「工作面」，记住分工就不会乱：

| 页面 | 它负责 | 通俗说 |
| --- | --- | --- |
| **分镜列表** | 队列 | 看本章镜头、处理批量生成/下载/诊断、进入指定镜头。 |
| **镜头详情** | 单镜头工作区 | 按「基础信息 / 提取确认 / 生成视频 / 视频结果」推进单镜头。 |
| **任务中心** | 监控 | 看所有后台任务跑得怎么样。 |

**主线：项目工作台进入分镜列表，再进入镜头详情四步完成准备与生成，最后在任务中心看运行状态。**

> 小提示：平台几乎处处都有「下一步该做什么」的引导按钮和提示，跟着走基本不会迷路。

---

## 3. 步骤一：项目列表页 —— 新建并管理作品

进入平台默认就是这里（左侧菜单「项目列表」）。每个项目是一张卡片。

### 顶部工具栏按钮

| 按钮 / 控件 | 功能 |
| --- | --- |
| **搜索框** | 按项目名称或描述搜索。 |
| **筛选标签**：全部 / 待补原文 / 待提取分镜 / 待准备镜头 / 生成中 / 可继续推进 | 按工作流阶段过滤项目，快速找到「卡在某一步」的作品。 |
| **排序**（最近更新 / 名称 A-Z / 章节数量）+ **↑↓** | 调整卡片排列顺序与升降序。 |
| **视图**（三个图标）| 切换卡片密度：网格 / 紧凑 / 大图列表。 |
| **批量** | 进入多选模式，勾选多个项目后出现「删除选中」做批量删除。 |
| **新建项目** | 弹窗创建新项目（见下）。 |

### 「新建项目」弹窗要填什么
- **名称、简介**
- **视觉风格**：`真人` 或 `动漫`
- **视频风格**：随视觉风格联动（如真人都市、国漫、水墨…）
- **默认视频比例**：`16:9`（横屏）或 `9:16`（竖屏短剧）——后续关键帧、视频都默认按它生成
- **种子 (seed)、是否统一风格** 等

创建后会自动进入该项目的工作台。

### 每张项目卡片上的按钮

| 按钮 | 功能 |
| --- | --- |
| **进入 / "下一步动作"按钮**（蓝色主按钮）| 智能跳转：平台根据项目当前阶段，直接带你去该做的那一步（比如「待补原文」→ 打开原文编辑；「待提取分镜」→ 去分镜列表）。 |
| **编辑**（铅笔图标）| 修改项目名称、风格、比例等。 |
| **删除**（垃圾桶图标）| 删除项目（不可恢复，会二次确认）。 |

卡片上还会显示**当前阶段标签**和三个统计：`待确认 / 已就绪 / 生成中` 的镜头数，让你一眼看清进度。

---

## 4. 步骤二：项目工作台 + 章节页 —— 录入剧本

点进项目后是 **项目工作台**，顶部有一排标签：
**章节**、**演员 / 角色 / 场景 / 道具 / 服装**（资产）、**文件**、**编辑**、**设置**。

录入剧本走 **「章节」** 标签。章节列表顶部会同时展示项目进度统计和资产健康快照。

### 章节标签上的按钮

| 按钮 | 功能 |
| --- | --- |
| **新建章节**（右上角）| 弹窗创建一章。 |
| 弹窗里 **「章节标题」** | 例如「第1集 出租屋里的争吵」。 |
| 弹窗里 **「章节内容」** | 直接粘贴剧本原文（也可以先建空章节，之后再补）。 |
| **创建** | 提交。创建后会弹提示问你「是否立即提取分镜」或「继续编辑原文」。 |

### 章节列表每行的按钮

| 按钮 | 功能 |
| --- | --- |
| **标题（可点）** | 点标题打开「原文编辑器」，补充/修改本章剧本。 |
| **主按钮**（每行最右，文字会变）| 智能引导：根据本章状态显示「编辑原文 / 提取分镜 / 准备镜头 / 查看进度」等，点了直接去对应页面或直接发起提取。 |
| **更多（···）下拉** | 包含「查看分镜」「编辑原文」等辅助入口，提取任务进行中时还有「取消分镜提取」。 |

列表里还会显示每章的**分镜数、准备状态、分镜流转（待确认/已就绪/生成中）**。

---

## 5. 步骤三：分镜列表页 —— 一键拆出镜头

从章节进来后到「分镜列表」。这是把整段剧本切成一个个镜头的地方。

### 顶栏按钮

| 按钮 | 功能 |
| --- | --- |
| **返回章节列表** | 回上一层。 |
| **处理当前镜头 / 继续当前镜头** | 快速进入镜头详情，回到上次停留的镜头步骤。 |
| **批量生成 / 批量下载 / 批量诊断** | 在章节维度统一处理可生成镜头与结果。 |

### 卡片右上角操作区按钮

| 按钮 | 功能 |
| --- | --- |
| **一键提取分镜并自动准备**（核心按钮）| 调用 AI 把本章剧本自动切成多个镜头，并顺带准备资产/对白。⚠️ 只有当前**没有分镜时**才能点；已有分镜想重提取，要先清空。 |
| **创建分镜** | 手动新建一个空镜头（填标题、剧本摘录）。 |
| **刷新** | 重新拉取列表。 |
| **取消提取**（提取任务运行时出现，红色）| 中止正在跑的拆分任务。 |
| **批量删除**（勾选了行才出现）| 删除选中的多个镜头。 |

### 列表区控件

- **搜索框**：按序号、标题、剧本摘录搜。
- **筛选标签**：全部 / 待确认 / 生成中 / 已就绪（带数量）。
- 勾选若干行后可继续做批量动作；单镜头处理仍以进入镜头详情为主。

### 每行（每个镜头）的操作按钮

| 按钮 | 功能 |
| --- | --- |
| **编辑 / 查看详情** | 进入「镜头详情」处理当前镜头（最常用，见下一步）。 |
| **插入** | 下拉「向上插入 / 向下插入」，在该镜头前后插入新镜头。 |
| **删除** | 删除该镜头（二次确认）。 |

> 拆分是**后台异步任务**，发起后可在左下角任务中心看进度，完成后镜头自动出现在列表里。

---

## 6. 步骤四：镜头详情页 —— 基础信息 / 提取确认 / 生成视频 / 视频结果

这是**最需要你动手**的一步，目标是把镜头从「待确认」推到「已就绪」。

镜头详情现在分成四个步骤：**基础信息 / 提取确认 / 生成视频 / 视频结果**。前两个步骤负责准备，后两个步骤负责生成与回看。

左侧镜头列表也有两套直接操作方式：

- **勾选框多选**：勾选后顶部出现批量工具条
- **右键单条镜头**：直接打开这条镜头的快捷操作菜单

如果你已经勾选了多条镜头，再去**右键其中任意一条已选镜头**，菜单动作会直接作用于这整组选中项。

要让一个镜头变「已就绪」，需完成下面这份**检查清单（4 项全绿）**：

### ① 基础信息与镜头语言
- **标题、剧本摘录**：确认/修正镜头文字。
- **镜头语言**：景别、机位角度、运镜、**时长**（⚠️ 时长决定后面生成视频的长度）。
- 填好点 **保存**（保存基础信息与镜头语言）。

### ② 动作拍点
确认这个镜头里发生的动作变化序列（谁做了什么），用于让生成画面更准确。

### ③ 资产确认（角色 / 场景 / 道具 / 服装）
主路径里，章节分镜提取完成后系统会**自动准备**资产 / 对白候选，并尽量自动关联可用资产。你在这里的主要工作是检查自动准备结果、修正遗漏和处理待确认项。

如果自动准备结果不完整，才使用单镜头重新提取或其他修复入口补数据。每条候选用以下按钮处理：

| 按钮 | 功能 |
| --- | --- |
| **关联** | 确认这条候选对应资产库里某个已有资产。系统会自动按名字匹配，存在同名资产时会弹窗让你确认。 |
| **新建** | 库里没有同名资产时，弹窗确认后会**新开一个标签页**到资产编辑页让你新建（自动带入名称、描述、项目风格）；建完切回来会自动刷新。 |
| **替换** | 把已关联的资产换成另一个（打开抽屉重选）。 |
| **忽略** | 这条候选用不上，丢弃。 |
| **添加关联**（按类别）| 主动给镜头补关联一个资产（候选没识别到时用）。 |

> 角色候选会**尝试自动关联**到项目里已有的同名角色，省去手动操作。

### ④ 对白确认
AI 从原文提取对白候选（含说话人）。每条可：

| 按钮 | 功能 |
| --- | --- |
| **接受** | 写入正式对白行（接受前可先编辑文字）。 |
| **忽略** | 丢弃。 |
| **全部接受 / 全部忽略** | 一键批量处理所有对白候选。 |

### 其他实用按钮
- **跳过提取**：本镜头确实不需要提取（如纯空镜）时点它，镜头直接变「已就绪」。
- **保存**：保存当前编辑。
- 完成全部确认后，系统会**自动**把镜头标为「已就绪」或显示「准备完成」，并提示你继续到「生成视频」步骤。

> 自动判定规则：所有资产候选都已「关联/忽略」**且**所有对白候选都已「接受/忽略」→ 自动「已就绪」。你**不用手动改状态**。

> 注意：**「已就绪 / 准备完成」不等于立刻能生成视频。** 是否能点生成，还要看「生成视频」步骤里的视频准备度诊断。

---

## 7. 步骤五：资产页 —— 常用一致性补强环节

这一步是保证一致性的常用辅助环节，不是主路径的硬性前置。入口：项目工作台的资产标签，或左侧全局 **「资产管理」**（顶部标签：演员 / 角色 / 场景 / 道具 / 服装）。

进入某个资产的**编辑页**后，标准操作和按钮如下：

| 区域 / 按钮 | 功能 |
| --- | --- |
| **名称、描述** | 填写资产的名字和外貌/风格/环境等文字描述。描述越具体，出图越准。 |
| **智能检测 / 智能补全** | AI 分析你的描述「还缺哪些关键信息」（如人物的发型、场景的光线），并给出优化后的可生成描述，可一键「填入描述」。 |
| **生成** | 用图片模型按当前描述（+ 可选参考图）出图，发起后台任务。 |
| **参考图输入框**（支持 `@`）| 输入 `@` 可从候选池里选图作为参考图。 |
| **候选池 / 图片候选池** | 所有生成或上传的图都汇集在这里。 |
| **上传**（到候选池）| 手动上传外部图片进候选池。 |
| **采用 / 选用**（候选图上的按钮）| 把满意的那张设为该资产的「当前正式图」。 |
| **移除**（候选图上的按钮）| 从候选池删掉某张候选图。 |

**角色 vs 演员**：演员是可跨项目复用的形象底库；角色是本项目人物，可绑定一个演员，让多个角色共享统一形象。

> **黄金原则：先把资产图定下来，再去出镜头关键帧。**
> 一致性靠这条链路逐层传递：**资产正式图 → 镜头关键帧 → 视频**。资产图是锚点，锚点稳了画面才稳。

---

## 8. 步骤六：镜头详情 —— 生成视频 + 查看结果

镜头变成「已就绪 / 准备完成」后，继续留在当前镜头详情，切到 **「生成视频」** 和 **「视频结果」** 两个步骤。这才是当前主流程里的生成工作区。

如你从旧链接进入 `/studio`，系统也会把你兼容跳转到相应镜头的生成相关步骤；它不是当前主流程入口。

### 8.1 先看「视频准备度」
「生成视频」步骤会显示这个镜头当前能不能出视频、还缺什么。章节级多镜头诊断则在分镜列表顶部工具栏里统一执行，未通过的镜头会在批量生成时自动跳过。

### 8.2 出关键帧（视频的参考画面）
关键帧分三种：**首帧**、**尾帧**、**关键帧**。在镜头详情的「生成视频」步骤里：

| 控件 / 按钮 | 功能 |
| --- | --- |
| 选 **帧类型**（首帧/尾帧/关键帧）| 选择要生成哪一帧。 |
| **关联角色 / 场景 / 道具 / 服装** | 给这一帧挂上参考资产，它们的正式图会作为参考图（顺序有效）。 |
| **（自动）渲染提示词** | 平台自动套用本镜头的运镜、构图、连贯性等约束生成提示词，你可手动微调。 |
| **分辨率档位** | 选关键帧输出清晰度档位。 |
| **生成**（关键帧）| 发起出图任务，按当前视频画幅比例生成，以提升后续视频稳定性。 |

### 8.3 生成视频
在「生成视频」步骤里配置参数后发起：

| 控件 / 参数 | 功能 / 说明 |
| --- | --- |
| **视频提示词** | 可手填或由平台生成；纯文本模式下必填。可附「氛围描述」。 |
| **参考模式** | 决定用哪些关键帧作参考，**参考图数量必须严格匹配**（见下表）。 |
| **视频比例** | 默认用项目比例，也可在镜头级覆盖。 |
| **时长** | ⚠️ **不在这里填**，由镜头的「时长」自动决定（要改去镜头详情的「基础信息」步骤改时长）。 |
| **生成视频** | 发起后台视频生成任务。 |

参考模式对照表：

| 参考模式 | 用什么参考 | 需要几张图 |
| --- | --- | --- |
| 纯文本 `text_only` | 只用文字 | 0（提示词必填）|
| `first` / `last` / `key` | 单张关键帧 | 1 |
| `first_last` | 首帧 + 尾帧 | 2 |
| `first_last_key` | 首 + 尾 + 关键帧 | 3 |

### 8.4 批量与下载按钮
- **批量生成**：在分镜列表顶部工具栏，一次给章节内多个镜头发起视频生成。
- **批量下载**：在分镜列表顶部工具栏，把已选中的视频一次性下载到本地文件夹。
- **视频结果**：回到当前镜头的「视频结果」步骤，一个镜头可能生成多个版本，点 **「√」** 选定要采用的版本。
- 视频结果里还有 **全屏播放 / 下载** 等小按钮。

---

## 9. 步骤七：任务中心 —— 看进度、拿成片

「拆镜头、提取、出图、出视频」全是**后台异步任务**。平台左下角常驻 **任务中心**：

| 你能做的 | 说明 |
| --- | --- |
| **看状态** | 排队 / 运行中 / 成功 / 失败，完成会有通知提示。 |
| **点开看结果或失败原因** | 失败会给出错误信息。 |
| **取消** | 运行中的任务通常可点取消。 |

> 任务中心只看「任务跑得怎么样」；具体成果回到对应资产页 / 镜头详情查看与下载。

---

## 10. 界面导航速查

**左侧主菜单（全局常驻）**：
`项目列表` · `资产管理` · `提示词模板` · `模型管理` · `设置`

**顶部流程导航（进项目后出现，体现进度锁定）**：
`主页面` → `项目工作台` → `分镜列表` → `镜头详情`
- 进项目解锁「项目工作台」；到达过章节后解锁「分镜列表 / 镜头详情」。
- 平台会记住每个项目最后访问的章节，方便快速跳回。

---

## 11. 常见问题 FAQ

**Q1：镜头一直「待确认」，进不了生成步骤？**
还有资产候选或对白候选没处理。回镜头详情的「提取确认」，把每条候选「关联/接受」或「忽略」；纯空镜可点「跳过提取」。

**Q2：同一个角色在不同镜头里换脸了？**
确保所有镜头都关联**同一个角色资产**，且该角色已生成并「采用」了正式形象图，再去出关键帧。一致性靠「资产图 → 关键帧 → 视频」逐层传递。

**Q3：「一键提取分镜」按钮是灰的点不了？**
本章已经有分镜了。想重新提取，需先把现有分镜清空（删除）。

**Q4：生成视频提示参考图数量不对？**
按模式匹配：单帧模式各 1 张，首尾帧 2 张，首尾关键帧 3 张，纯文本不需图但提示词必填。

**Q5：视频时长怎么改？**
不在「生成视频」步骤里填，去镜头详情的「基础信息」改该镜头的「时长」。

**Q6：剧本里角色称呼混乱怎么办？**
在剧本/章节阶段用「一致性检查」找出角色混淆问题，再用「剧本优化」自动修订。

---

### 一句话总结

> **建项目 → 录入剧本 → 进入分镜列表 → 在镜头详情四步里完成准备 → 在镜头详情生成视频并查看结果。**
> 资产管理和资产定图是保证一致性的常用补强环节，需要时穿插处理即可。
</content>
