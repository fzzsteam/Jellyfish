# Jellyfish 使用手册（新手主流程详解）

> Jellyfish 是一个 **AI 驱动的短剧 / 短视频生产工作台**。
> 你只要把一段剧本文字放进去，平台就能带你一步步走完：
> **录入剧本 → 自动拆分镜头 → 确认每个镜头的角色/场景/道具/对白 → 给资产出定妆图 → 给镜头出关键帧 → 生成视频**。
> 它最核心的价值是「**资产一致性**」：保证同一个角色、同一个场景在所有镜头里长得一样、不跑偏。
>
> 本手册聚焦**主流程功能**，看完即可独立上手操作。

---

## 目录

1. [先理解 5 个核心概念](#1-先理解-5-个核心概念)
2. [主流程全景图](#2-主流程全景图)
3. [前置准备：配置 AI 模型](#3-前置准备配置-ai-模型)
4. [步骤一：创建项目](#4-步骤一创建项目)
5. [步骤二：录入剧本 → 自动拆分镜头](#5-步骤二录入剧本--自动拆分镜头)
6. [步骤三：分镜准备（最关键的人工确认环节）](#6-步骤三分镜准备最关键的人工确认环节)
7. [步骤四：资产管理与出图（一致性的根基）](#7-步骤四资产管理与出图一致性的根基)
8. [步骤五：分镜工作室（出关键帧 + 生成视频）](#8-步骤五分镜工作室出关键帧--生成视频)
9. [步骤六：任务中心（追踪所有生成任务）](#9-步骤六任务中心追踪所有生成任务)
10. [界面导航速查](#10-界面导航速查)
11. [常见问题 FAQ](#11-常见问题-faq)

---

## 1. 先理解 5 个核心概念

把这套层级关系理解了，整个平台就通了：

```
项目 (Project)          一部作品
 └─ 章节 (Chapter)       一段完整剧本，比如「第一集」
     └─ 镜头 (Shot)      剧本被拆出来的一个个分镜
         ├─ 镜头细节：景别、机位、运镜、时长、动作拍点
         ├─ 对白：这个镜头里的台词
         └─ 关键帧：首帧 / 尾帧 / 关键帧
```

**资产**是跨镜头复用的素材库，是保证一致性的关键：

| 资产 | 含义 |
| --- | --- |
| **角色 (Character)** | 剧中人物（项目内），带形象图。 |
| **演员 (Actor)** | 可跨项目复用的「形象底库」，角色可绑定演员共用同一张脸。 |
| **场景 (Scene)** | 故事发生的地点环境。 |
| **道具 (Prop)** | 镜头里出现的物件。 |
| **服装 (Costume)** | 角色的服装 / 造型。 |

### 两个状态词，务必分清

- **`shot.status`（镜头状态）**：只有 `pending`（待确认）和 `ready`（已就绪）两种。
  `ready` 表示「这个镜头的信息提取**确认工作做完了**」。
- **`video-readiness`（视频准备度）**：单独判断「这个镜头现在**能不能出视频**」（关键帧、参考图、参数是否齐）。

> 一句话：**`ready` = 准备做完了；`video-readiness` = 生成素材齐了。** 两者独立。

---

## 2. 主流程全景图

```
①配模型 → ②建项目 → ③录入剧本 → ④拆分镜头
   → ⑤分镜准备（确认角色/场景/道具/对白，推进到 ready）
   → ⑥资产出图（把每个角色/场景的图定下来）
   → ⑦分镜工作室（出关键帧 → 生成视频）
   → ⑧任务中心（看进度、下载成片）
```

平台把工作清晰地分成三个「工作面」，记住它们的分工就不会乱：

| 页面 | 职责 | 干什么 |
| --- | --- | --- |
| **分镜编辑页**（准备台） | 准备 | 提取并确认角色/对白、补齐基础信息，把镜头推进到 `ready`。 |
| **分镜工作室**（生成台） | 生成 | 出关键帧、配参考图、设视频参数、生成视频。 |
| **任务中心** | 监控 | 看所有异步任务跑得怎么样。 |

**主线就是：先「准备」，后「生成」，再「监控」。**

---

## 3. 前置准备：配置 AI 模型

> ⚠️ 这一步不做，后面所有「拆分、提取、出图、出视频」都会失败。

进入左侧菜单 **「模型管理」**，配置三样东西：

1. **供应商 (Provider)**：添加你的 AI 服务商账号（支持 **OpenAI / 阿里百炼 / 火山引擎 / Vidu** 等），填 API Key、接入地址。
2. **模型 (Model)**：在供应商下添加具体模型，并标注类别：
   - **文本模型 (text)** —— 拆剧本、提取信息、一致性检查
   - **图片模型 (image)** —— 资产出图、镜头关键帧
   - **视频模型 (video)** —— 镜头视频生成
3. **全局默认设置**：指定**默认文本模型 / 默认图片模型 / 默认视频模型**。各处生成时若不单独指定，就用这里的默认值。

> 最低要求：三类默认模型都要配齐，否则对应能力不可用。

---

## 4. 步骤一：创建项目

1. 左侧 **「项目列表」** → 右上角 **「新建项目」**。
2. 填写关键字段：
   - **名称、简介**
   - **画面表现形式**：`真人` 或 `动漫`
   - **题材风格**：随表现形式联动（如真人都市、国漫、水墨等）
   - **默认视频画幅比例**：`16:9`（横屏）或 `9:16`（竖屏短剧）—— 后续关键帧、视频都会默认按它生成
   - **种子 (seed)、是否统一风格** 等
3. 创建后点进项目卡片，进入 **项目工作台**。

**项目工作台顶部标签**：仪表盘（进度概览）、章节、演员、角色、场景、道具、服装、文件、编辑、设置。

> 项目列表支持按阶段筛选（待编辑原文 / 待提取分镜 / 待准备分镜 / 生成中 / 已就绪），一眼看出每个项目卡在哪一步。

---

## 5. 步骤二：录入剧本 → 自动拆分镜头

1. 项目工作台进入 **「章节」** 标签 → **新建章节**（如「第一集」）。
2. 进入章节，**粘贴剧本原文**。
3. 触发 **「拆分分镜」**：平台用文本模型把剧本自动切成一个个镜头，识别每个镜头的：序号、原文起止行、镜头名、画面摘要、时间（白天/夜晚）。
   - 这是**异步任务**，发起后在任务中心看进度；完成后镜头写入「分镜列表」。

**剧本阶段的可选 AI 工具**（按需用）：
- **一致性检查**：找出「同一角色被写成不同身份/称呼」的混淆问题并给修改建议。
- **剧本优化**：基于一致性检查结果，最小改动地修订剧本。
- **智能精简**：在保证剧情连贯前提下精简冗长剧本。

---

## 6. 步骤三：分镜准备（最关键的人工确认环节）

这一步把每个镜头从 `pending` 推进到 `ready`，是整条流程里**最需要你动手**的地方。

进入 **「分镜列表」**，点单个镜头进入 **分镜编辑页**。页面左侧是镜头列表（支持「全部 / 未提取 / 待确认」筛选，按住 Ctrl/Cmd 可多选批量操作），右侧是当前镜头的准备工作，分两个标签页：**基础信息** 与 **确认**。

准备一个镜头需要完成这份 **检查清单（4 项全绿才算 ready）**：

### ① 基础信息与镜头语言
- **标题、剧本摘录**：确认/修正镜头文字。
- **镜头语言**：景别 (camera_shot)、机位角度 (angle)、运镜 (movement)、**时长 (duration)**。
  > 时长很重要——后续生成视频的长度就由它决定。
- 填好后点保存。

### ② 动作拍点 (action beats)
确认这个镜头里的动作变化序列（角色/画面发生了什么），用于后续生成更准确的画面。

### ③ 资产确认（角色 / 场景 / 道具 / 服装）
点 **「提取」**（单镜头）或选中多个镜头 **批量提取**，平台从镜头原文识别出资产候选。每条候选要逐一处理：

| 操作 | 说明 |
| --- | --- |
| **关联 (link)** | 确认它对应资产库里某个已有资产。系统会自动按名字匹配，存在同名资产时弹窗让你确认关联。 |
| **新建** | 库里没有时，弹窗确认后会**新开标签页**到资产编辑页让你新建（自动带入名称、描述、项目风格）。建完切回来会自动刷新。 |
| **替换** | 把已关联的资产换成另一个。 |
| **忽略 (ignore)** | 这条候选不需要。 |

> 角色资产在准备时会**尝试自动关联**到项目里已有的同名角色，省去手动操作。

### ④ 对白确认
平台从原文提取对白候选（含说话人、对白模式）。每条可：
- **接受 (accept)**：写入正式对白行（可先编辑文字内容）。支持**一键全部接受**。
- **忽略 (ignore)**：丢弃。支持**一键全部忽略**。

### 自动推进到 ready
后端按规则自动重算状态，**你不用手动改状态**：

> - 所有资产候选都已 `linked` 或 `ignored`，**且** 所有对白候选都已 `accepted` 或 `ignored` → 自动变 **`ready`**
> - 镜头确实不需要提取（如纯空镜）→ 设置 **「跳过提取」**，直接 `ready`
> - 提取后没有任何候选 → 也直接 `ready`

镜头变 `ready` 后，页面会提示「下一步：进入分镜工作室继续生成」。

---

## 7. 步骤四：资产管理与出图（一致性的根基）

这一步决定成片里「人物/场景长得一不一致」。入口：项目工作台的资产标签，或左侧全局 **「资产管理」**。

对每个角色 / 演员 / 场景 / 道具 / 服装，标准操作是：

1. **完善文字描述**（外貌、风格、环境等）。
   平台提供 **AI 信息补全分析**（人物画像 / 场景 / 道具 / 服装），告诉你「还缺哪些关键描述」，并给出优化后的可生成描述。
2. **生成形象图 / 定妆图**：用图片模型按描述（+ 可选参考图）出图，异步任务。
3. **从候选图库挑图**：出多张后，在候选图库里挑满意的，**「采用 (adopt)」** 为该资产的当前正式图。
4. 这张正式图，会作为后续所有引用该资产的镜头关键帧的**参考图**。

**角色 vs 演员**：演员是可跨项目复用的形象底库；角色是本项目人物，可绑定一个演员，让多个角色共享统一形象基底。

> **黄金原则：先把资产图定下来，再去出镜头关键帧。**
> 一致性靠这条链路逐层传递：**资产图 → 镜头关键帧 → 视频**。资产图是锚点，锚点稳了，画面才稳。

---

## 8. 步骤五：分镜工作室（出关键帧 + 生成视频）

镜头进入 `ready` 后，进入 **「分镜工作室」**——这是**生成工作台**。

界面分三栏：**左侧镜头列表** · **中间视频预览/选择区** · **右侧属性面板（关键帧 + 视频参数）**。

### 8.1 先看视频准备度
工作室会显示每个镜头能不能出视频、还缺什么。可对选中的多个镜头做**批量准备度检查**，未通过的会被标出并自动跳过。

### 8.2 出关键帧（视频的参考画面）
关键帧分三种：**首帧 (first)**、**尾帧 (last)**、**关键帧 (key)**。操作：
1. 在右侧属性面板的「关键帧与参考图」里选帧类型。
2. 给该帧**关联角色 / 场景 / 道具 / 服装**——它们的资产图会作为参考图（顺序有效）。
3. 平台**自动渲染提示词**（套用镜头的运镜、构图、连贯性等约束模板）。你可微调。
4. 选**分辨率档位**，发起生成。关键帧会按当前视频画幅比例出图，以提升后续视频稳定性。

### 8.3 生成视频
在右侧配置参数后发起：
- **视频提示词**：可手填/由平台生成；纯文本模式下必填。还可加「氛围描述」。
- **参考模式 (reference_mode)**，参考图数量必须严格匹配：

  | 模式 | 用什么参考 | 需要几张图 |
  | --- | --- | --- |
  | `text_only` | 纯文字 | 0（提示词必填） |
  | `first` / `last` / `key` | 单张关键帧 | 1 |
  | `first_last` | 首帧+尾帧 | 2 |
  | `first_last_key` | 首+尾+关键帧 | 3 |

- **画幅比例**：默认用项目比例，也可在镜头级覆盖。
- **时长**：**不在此处填**，由镜头细节的 duration 自动决定。

### 8.4 批量与下载
- 支持**批量生成**章节内多个镜头的视频。
- 一个镜头可能生成多个视频版本，在中间预览区点「√」**选定采用的版本**。
- 支持把已选中的视频**批量下载**到本地文件夹。

---

## 9. 步骤六：任务中心（追踪所有生成任务）

「拆分镜、提取、出图、出视频」全是**异步任务**。平台右下角常驻 **任务中心**：
- 实时显示任务状态（排队 / 运行中 / 成功 / 失败），完成有通知。
- 点开看结果或失败原因；运行中的任务通常可取消。

> 任务中心只看「任务跑得怎么样」；具体业务结果回到对应的资产页 / 镜头工作室查看。

---

## 10. 界面导航速查

**左侧主菜单（全局常驻）**：
`项目列表` · `资产管理` · `提示词模板` · `模型管理` · `设置`

**顶部流程导航（进项目后出现，体现进度锁定）**：
`主页面` → `项目工作台` → `分镜列表` → `分镜工作室`
- 进项目解锁「项目工作台」；到达过章节后解锁「分镜列表 / 分镜工作室」。
- 平台会记住每个项目最后访问的章节，方便快速跳回。

---

## 11. 常见问题 FAQ

**Q1：点生成没反应/报错？**
先去「模型管理」确认对应类别（文本/图片/视频）模型已配置，且全局默认模型已设置。

**Q2：镜头一直 pending，进不了工作室？**
还有资产候选或对白候选没处理。回分镜编辑页把每条候选「关联/接受」或「忽略」；纯空镜可设「跳过提取」。

**Q3：同一角色在不同镜头里换脸了？**
确保所有镜头都关联**同一个角色资产**，且该角色已生成并「采用」正式形象图，再去出关键帧。一致性靠「资产图 → 关键帧 → 视频」逐层传递。

**Q4：生成视频提示参考图数量不对？**
按模式匹配：`first/last/key` 各 1 张，`first_last` 2 张，`first_last_key` 3 张，`text_only` 不需图但提示词必填。

**Q5：视频时长怎么改？**
不在生成处填，去改镜头细节 (ShotDetail) 的 duration。

**Q6：剧本里角色称呼混乱？**
用剧本阶段的「一致性检查」找问题，再用「剧本优化」自动修订。

---

### 一句话总结

> **配模型 → 建项目 → 拆剧本 → 确认分镜信息（推到 ready）→ 把资产图定下来 → 出关键帧 → 生成视频。**
> 牢牢抓住「资产一致性」这条主线，产出的短剧画面就能稳定不跑偏。
</content>
