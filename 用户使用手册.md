# Jellyfish 使用手册（新手入门）

> Jellyfish 是一个 **AI 驱动的短剧（短视频/动漫）生产工作台**。
> 你只需要把一段剧本文字丢进去，平台就能帮你一步步完成：
> **剧本拆分 → 分镜（镜头）拆解 → 角色/场景/道具/服装资产管理 → 关键帧出图 → 视频生成**，
> 并用「资产一致性」机制保证同一个角色/场景在不同镜头里长得一样。
>
> 它对标的是「巨日禄」这类一键成片工具，但把整条流水线拆得更细、每一步都可人工干预，
> 更适合追求画面一致性和可控性的创作者。

---

## 目录

1. [平台能做什么](#1-平台能做什么)
2. [核心概念（先理解这几个词）](#2-核心概念先理解这几个词)
3. [整体生产流程总览](#3-整体生产流程总览)
4. [第一步：把平台跑起来（部署）](#4-第一步把平台跑起来部署)
5. [第二步：配置 AI 模型（必做，否则无法生成）](#5-第二步配置-ai-模型必做否则无法生成)
6. [第三步：创建项目](#6-第三步创建项目)
7. [第四步：录入剧本并拆分章节 / 分镜](#7-第四步录入剧本并拆分章节--分镜)
8. [第五步：分镜准备（确认角色/场景/道具/对白）](#8-第五步分镜准备确认角色场景道具对白)
9. [第六步：资产管理与出图（保证一致性的核心）](#9-第六步资产管理与出图保证一致性的核心)
10. [第七步：分镜工作室（关键帧 + 视频生成）](#10-第七步分镜工作室关键帧--视频生成)
11. [第八步：任务中心（查看生成进度）](#11-第八步任务中心查看生成进度)
12. [其他功能模块](#12-其他功能模块)
13. [界面导航速查](#13-界面导航速查)
14. [常见问题 FAQ](#14-常见问题-faq)

---

## 1. 平台能做什么

| 能力 | 说明 |
| --- | --- |
| **剧本智能拆分** | 把整段剧本文本自动切成一个个「镜头（分镜）」，识别每个镜头的起止、时间、画面摘要。 |
| **信息自动提取** | 从每个镜头里自动识别出现的 **角色、场景、道具、服装、对白台词**，生成「候选项」让你确认。 |
| **资产一致性管理** | 为角色/场景/道具/服装建立可复用的「资产」，并生成统一的定妆图/参考图，保证跨镜头长相一致。 |
| **AI 出图** | 为资产生成形象图、为镜头生成关键帧（首帧/尾帧/关键帧）。 |
| **AI 视频生成** | 基于关键帧 + 提示词，调用视频模型生成镜头视频片段。 |
| **剧本辅助工具** | 一致性检查（角色混淆检测）、剧本优化、智能精简等。 |
| **任务追踪** | 所有出图/出视频都是异步任务，可在任务中心统一查看进度与结果。 |

支持的 AI 供应商：**OpenAI、阿里百炼（通义万相等）、火山引擎（豆包/即梦等）**。

---

## 2. 核心概念（先理解这几个词）

理解下面这套层级关系，整个平台就通了：

```
项目 (Project)
 └── 章节 (Chapter)          ← 一段完整剧本，例如「第一集」
      └── 分镜 / 镜头 (Shot)  ← 剧本被拆出来的一个个镜头
           ├── 镜头细节 (ShotDetail)：景别、时长、运镜、场景等
           ├── 对白 (DialogLine)：这个镜头里的台词
           └── 关键帧 (FrameImage)：首帧 first / 尾帧 last / 关键帧 key
```

而「资产」是**跨镜头复用**的素材库：

| 资产类型 | 含义 |
| --- | --- |
| **角色 (Character)** | 剧中人物（项目内的角色，带形象图）。 |
| **演员 (Actor)** | 可复用的「演员形象库」，类似一个底模/真人形象，角色可以绑定演员。 |
| **场景 (Scene)** | 故事发生的地点环境。 |
| **道具 (Prop)** | 镜头里出现的物件。 |
| **服装 (Costume)** | 角色的服装/造型变体。 |

> 一致性的关键就在于：同一个角色/场景/道具在所有镜头里都引用**同一份资产 + 同一张参考图**，
> 这样 AI 生成时画面才不会「换脸」「换景」。

### 两个关键状态概念

- **`shot.status`（镜头状态）**：只有两种
  - `pending`（待确认）：信息提取还没确认完
  - `ready`（已就绪）：信息已确认，**具备进入视频生成的前提条件**
- **`video-readiness`（视频生成准备度）**：单独判断「这个镜头现在能不能出视频」（关键帧、参考图、参数是否齐全），与 `shot.status` 分离。

> 简单记：**`ready` = 准备工作做完了；`video-readiness` = 生成所需素材齐了。**

---

## 3. 整体生产流程总览

```
①配置模型 → ②建项目 → ③录入剧本 → ④拆分镜 → ⑤分镜准备(确认角色/场景/道具/对白)
   → ⑥资产出图(保证一致性) → ⑦分镜工作室(出关键帧→出视频) → ⑧任务中心查看结果
```

对应到页面，平台把工作划分成清晰的三个「工作面」：

| 页面 | 职责 | 一句话 |
| --- | --- | --- |
| **分镜列表 / 分镜编辑页** | 准备 | 提取并确认角色/对白，修正基础信息，把镜头推进到 `ready`。 |
| **分镜工作室 (ChapterStudio)** | 生成 | 出关键帧、配参考图、设视频参数、发起视频生成。 |
| **任务中心 (TaskCenter)** | 监控 | 查看所有异步任务的状态与结果。 |

记住这条主线：**先「准备」，后「生成」，再「监控」。**

---

## 4. 第一步：把平台跑起来（部署）

### 环境要求
- Node.js + pnpm（前端）
- Python 3.11+ 与 `uv`（后端）
- Docker / Docker Compose（用于 MySQL、Redis、RustFS 文件存储）

### 方式 A：一键全栈（最省事，推荐第一次体验）

```bash
cp deploy/compose/.env.example deploy/compose/.env
docker compose --env-file deploy/compose/.env -f deploy/compose/docker-compose.yml up --build
```

启动后端口：
- 前端：`7788`
- 后端：`8000`
- MySQL `3306`、Redis `6379`、RustFS（文件存储）`9000`

浏览器打开 **`http://localhost:7788`** 即可进入平台。

### 方式 B：本地开发模式（基础设施用 Docker，前后端跑在本机，便于热更新）

```bash
# 1. 启动基础设施（MySQL / Redis / RustFS）
cp deploy/compose/.env.local.example deploy/compose/.env.local
docker compose --env-file deploy/compose/.env.local -f deploy/compose/docker-compose.infra.yml up -d

# 2. 启动后端
cd backend
uv sync
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 3. 启动前端
cd front
pnpm install
pnpm dev
```

常用地址：
- 前端：`http://localhost:7788`
- 后端 API：`http://localhost:8000`
- 接口文档 Swagger：`http://localhost:8000/docs`

> 本地模式下若 3306/6379 被占用，会自动改用 3307/6380，后端会同步连接。

---

## 5. 第二步：配置 AI 模型（必做，否则无法生成）

进入左侧菜单 **「模型管理」**。这里分三类内容：

### 5.1 供应商（Provider）
先添加你拥有的 AI 服务商账号，例如 **OpenAI / 阿里百炼 / 火山引擎**，填入：
- 供应商类型
- API Key
- （如需）Base URL / 接入地址

### 5.2 模型（Model）
在供应商下添加具体模型，并标明类别：
- **文本模型 (text)**：用于剧本拆分、信息提取、一致性检查等。
- **图片模型 (image)**：用于角色/场景/道具出图、镜头关键帧。
- **视频模型 (video)**：用于镜头视频生成。

### 5.3 全局默认设置（Model Settings）
在「设置」标签里指定**默认文本模型 / 默认图片模型 / 默认视频模型**。
平台在各处出图/出视频时，如果没单独指定模型，就会用这里的默认值。

> ⚠️ 必须至少配置好「默认文本模型 + 默认图片模型 + 默认视频模型」，
> 否则对应的拆分、出图、出视频功能会报错。

---

## 6. 第三步：创建项目

1. 左侧菜单点 **「项目列表」**（主页面）。
2. 点右上角 **「新建项目」**，填写：
   - **项目名称、简介**
   - **画面表现形式 (visual_style)**：`真人` 或 `动漫`
   - **题材/风格 (style)**：根据上面的表现形式联动可选（如真人写实、动漫国漫、水墨等）
   - **默认视频画幅比例**：如 `16:9`（横屏）/ `9:16`（竖屏短剧）
   - **种子 (seed)、是否统一风格 (unify_style)** 等
3. 创建后点进项目卡片，进入 **项目工作台**。

> 项目列表支持按工作流阶段筛选（全部 / 待编辑原文 / 待提取分镜 / 待准备分镜 / 生成中 / 已就绪），
> 方便你快速找到「卡在哪一步」的项目。

### 项目工作台标签页
进入项目后，顶部有这些标签：

| 标签 | 用途 |
| --- | --- |
| **仪表盘** | 项目整体进度概览。 |
| **章节** | 管理章节、录入剧本、拆分镜的入口。 |
| **演员 / 角色 / 场景 / 道具 / 服装** | 本项目的各类资产管理。 |
| **文件** | 项目相关上传的文件/图片。 |
| **编辑 / 设置** | 项目编辑器与项目级设置。 |

---

## 7. 第四步：录入剧本并拆分章节 / 分镜

1. 在项目工作台进入 **「章节」** 标签。
2. **新建章节**（例如「第一集」），章节是承载一段完整剧本的单位。
3. 进入章节，**录入剧本原文**（粘贴整段剧本文字）。
4. 触发 **「拆分分镜」**：
   - 平台调用文本模型，把剧本自动切成一个个镜头（Shot）。
   - 每个镜头会识别出：序号、原文起止行、镜头名、画面摘要、时间（白天/夜晚）等。
   - 这是**异步任务**，发起后可在任务中心看进度，完成后镜头会写入「分镜列表」。

> 拆分完成后，章节下就有了一串镜头，接下来逐个「准备」它们。

### 可选的剧本辅助工具
在剧本阶段平台还提供几个 AI 工具（按需使用）：
- **一致性检查**：检测剧本里「同一角色被写成不同身份/称呼」导致的混淆，并给修改建议。
- **剧本优化**：基于一致性检查结果，最小化改动地修订剧本。
- **智能精简**：在保证剧情连贯前提下精简冗长剧本。

---

## 8. 第五步：分镜准备（确认角色/场景/道具/对白）

这是整条流程里**最需要人工参与**的一步，目标是把每个镜头从 `pending` 推进到 `ready`。

进入 **「分镜列表」**，点单个镜头进入 **分镜编辑页（单镜头准备台）**。在这里你会做：

### 8.1 信息提取
触发提取后，平台从镜头原文里识别出：
- **资产候选**：角色 / 场景 / 道具 / 服装
- **对白候选**：台词，含说话人、对话模式等

### 8.2 确认候选项
每条候选项都有状态，需要你逐一处理：

**资产候选**（角色/场景/道具/服装）：
- **关联 (link)**：确认它对应某个已有资产（或新建资产再关联）。
- **忽略 (ignore)**：这条不需要。

**对白候选**：
- **接受 (accept)**：写入正式对白行 ShotDialogLine。
- **忽略 (ignore)**：丢弃。

### 8.3 推进到 ready
后端会自动按规则重算 `shot.status`：

> - 所有资产候选都已 `linked` 或 `ignored`
> - 且所有对白候选都已 `accepted` 或 `ignored`
> → 镜头自动变为 **`ready`**

特殊情况：
- 若某镜头确实不需要提取（如纯空镜），可设置 **「跳过提取 (skip_extraction)」**，镜头直接变 `ready`。
- 若提取后没有任何候选项，也会直接 `ready`。

> 💡 角色资产在准备时会尝试**自动关联**到项目中已有的同名角色，减少手动操作。

---

## 9. 第六步：资产管理与出图（保证一致性的核心）

这一步决定了你的成片「人物/场景长得一不一致」。可以在项目工作台的资产标签，或左侧 **「资产管理」** 全局页进行。

### 9.1 资产的统一操作
对每个角色 / 演员 / 场景 / 道具 / 服装：
1. 打开资产编辑页。
2. 完善它的**文字描述**（外貌、风格、环境等）。平台还提供 **AI 信息补全分析**：
   - 人物画像缺失分析、场景信息分析、道具信息分析、服装信息分析
   - 它会告诉你「还缺哪些关键描述」，并给出优化后的可生成描述。
3. **生成形象图 / 定妆图**：
   - 平台用图片模型，根据描述（+ 可选参考图）出图。
   - 这是异步任务，出多张后可在**候选图库**里挑选，把满意的一张**采用 (adopt)** 为当前正式图。
4. 该资产的正式图，会作为后续所有引用它的镜头关键帧的**参考图**，从而保证一致性。

### 9.2 角色与演员
- **演员（Actor）** 是可跨项目复用的形象底库；
- **角色（Character）** 是本项目剧中人物，可绑定一个演员形象，让多个角色共享统一的真人/形象基底。

> 关键原则：**先把资产图定下来，再去生成镜头关键帧**。
> 资产图是「锚点」，镜头关键帧引用资产图，视频再引用关键帧，一致性就这样一层层传递下去。

---

## 10. 第七步：分镜工作室（关键帧 + 视频生成）

当镜头进入 `ready` 后，进入 **「分镜工作室」(ChapterStudio)** ——这是**生成工作台**。

### 10.1 查看视频准备度 (video-readiness)
工作室会显示每个镜头「能不能出视频」，以及还缺什么前置条件（关键帧/参考图/参数）。

### 10.2 生成关键帧
镜头关键帧分三类：
- **首帧 (first)**、**尾帧 (last)**、**关键帧 (key)**

操作：
1. 选择帧类型，平台会**渲染提示词**（自动套用镜头的运镜、构图、连贯性约束等模板，并把关联资产的图作为参考图）。
2. 你可微调提示词与参考图。
3. 发起生成，得到关键帧图片（按目标视频画幅比例出图，以提升后续视频稳定性）。

### 10.3 生成视频
配置视频参数后发起生成：
- **参考模式 (reference_mode)**：
  - `text_only`：纯文字提示词生成（此时提示词必填）
  - `first` / `last` / `key`：用单张关键帧作参考
  - `first_last` / `first_last_key`：用多张关键帧组合作参考
  - > 参考图数量必须与所选模式严格匹配。
- **画幅比例 (ratio)**：如 16:9 / 9:16。
- **时长**：由镜头细节 ShotDetail 的 duration 自动决定，无需手填。

发起后同样是异步任务，可在任务中心 / 工作室里追踪，完成后得到该镜头的视频片段。

### 10.4 批量推进
工作室支持在章节内**连续/批量**推进多个镜头的生成，适合一集几十个镜头的批处理。

---

## 11. 第八步：任务中心（查看生成进度）

所有「拆分镜、提取、出图、出视频」都是**异步任务**。平台右下角常驻 **任务中心 (TaskCenter)**：
- 实时显示任务状态（排队 / 运行中 / 成功 / 失败）。
- 点开可查看结果或失败原因。
- 任务完成会有通知提示。

> 任务中心是**通用状态面板**，只看「任务跑得怎么样」；具体业务结果回到对应资产/镜头页面查看。

---

## 12. 其他功能模块

| 模块（左侧菜单） | 用途 |
| --- | --- |
| **提示词模板** | 管理出图/出视频用的 Prompt 模板，可统一调整生成风格与约束。 |
| **文件管理** | 上传/管理图片等素材文件（底层用 RustFS 对象存储）。 |
| **智能体 (Agents)** | 查看/编辑平台内置的各类 AI Agent（剧本拆分、实体提取、一致性检查等）。 |
| **视频编辑器 (Editor)** | 对生成结果做后续编排/剪辑（项目级入口）。 |
| **设置** | 个人/系统设置。 |

---

## 13. 界面导航速查

**左侧主菜单**（全局，始终可见）：
`项目列表` · `资产管理` · `提示词模板` · `模型管理` · `设置`

**顶部流程导航**（进入项目后出现，体现进度锁定）：
`主页面` → `项目工作台` → `分镜列表` → `分镜工作室`
- 进入项目后解锁「项目工作台」；
- 到达过章节后解锁「分镜列表 / 分镜工作室」；
- 平台会记住每个项目最后访问的章节，方便快速跳回。

---

## 14. 常见问题 FAQ

**Q1：点了生成没反应 / 报错？**
先检查「模型管理」里是否配置了对应类别（文本/图片/视频）的模型，以及全局默认模型是否已设置。

**Q2：镜头一直停在 pending，进不了工作室？**
说明还有资产候选或对白候选没处理。回到分镜编辑页，把每条候选都「关联/接受」或「忽略」；或对该镜头设置「跳过提取」。

**Q3：同一个角色在不同镜头里长得不一样？**
确保所有镜头都关联到**同一个角色资产**，且该角色已生成并「采用」了正式形象图，再去出镜头关键帧。一致性靠「资产图 → 关键帧 → 视频」逐层传递。

**Q4：视频生成提示参考图数量不对？**
参考图数量必须与 `reference_mode` 匹配：`first/last/key` 各需 1 张；`first_last` 需 2 张；`first_last_key` 需 3 张；`text_only` 不需要参考图但提示词必填。

**Q5：视频时长怎么设？**
不在生成请求里设，由镜头细节 (ShotDetail) 的 duration 决定。需要改时长去改镜头细节。

**Q6：剧本里角色称呼混乱怎么办？**
用剧本阶段的「一致性检查」找出角色混淆问题，再用「剧本优化」自动修订。

---

### 一句话总结

> **配模型 → 建项目 → 拆剧本 → 确认分镜信息 → 把资产图定下来 → 出关键帧 → 出视频。**
> 牢牢抓住「资产一致性」这条主线，你产出的短剧画面就能稳定不跑偏。
</content>
</invoke>
